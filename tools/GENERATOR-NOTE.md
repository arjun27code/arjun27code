# Generator note

The supplied light and dark SVG files were used as layout references and code templates.

Changes made:

- Removed the original person's portrait and traveller data.
- Segmented Arjun from his original photograph.
- Generated a fresh 300×340 Floyd-Steinberg dither portrait.
- Added a 60-group global shimmer intro.
- Added a dense portrait layer.
- Added 900 independently animated traveller dots.
- Matched traveller destinations with optimal transport:
  portrait → n8n → Supabase → Security → portrait.
- Replaced all visible identity, role, location, stack, email and GitHub values.

The deployed `dark.svg` and `light.svg` contain the portrait and animation as SVG code.
